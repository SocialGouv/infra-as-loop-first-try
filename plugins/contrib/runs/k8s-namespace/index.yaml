# run in tmpdir, all content of the run in ./
vars:
  name:
test:
retry: 2
# testBefore:
# testAfter:
# exec: | # gomplate using .vars, .options
#   node ./run.js
# if no run specified here but run.js, run.js will be invoked with {vars, options}
# test: { use: "namespace-exists" }